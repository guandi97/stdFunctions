/*
 *Guandi97
 *Assembly recreation of some stdlib functions
 */

#include <unistd.h>

#define asmfun

int sterlen(char*);
int ati(char*);
int memcp(char*,char*,size_t);

int sterlen(char* str)
{
}
int ati(char* str)
{
}
memcp(char *data,char *target,size_t size)
{
}

